spring:
  datasource:
    url: jdbc:mysql://localhost:3307/wonmally_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: wonmally
    password: ${DB_PASSWORD:changeme}
  jpa:
    show-sql: true

wonmally:
  security:
    jwt:
      secret: ${JWT_SECRET:?Veuillez definir JWT_SECRET dans votre environnement}

logging:
  level:
    com.wonmally.app: DEBUG
    org.springframework.security: DEBUG